import 'package:flutter/material.dart';

class TechnicianHomePage extends StatelessWidget {
  const TechnicianHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Center(
        child: const Text('Welcome to the Technician Home Page!'),
      ),
    );
  }
}
